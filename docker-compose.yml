version: "3.8"
services:
  api:
    build: ./server
    ports:
      - "4000:4000"
    volumes:
      - ./server/uploads:/app/uploads
      - ./server/data:/app/data
    environment:
      - NODE_ENV=development
  web:
    build: ./web
    ports:
      - "5173:5173"
    depends_on:
      - api
